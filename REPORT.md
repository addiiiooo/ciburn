# REPORT.md — GitHub Actions waste across a public corpus

Generated by `scripts/build_report.py` (ciburn 0.1.0) on 2026-09-12T09:19:19Z. Every number below is computed from the files in `dataset/`; re-run the script to regenerate this file byte for byte. Nothing here was typed in by hand.

## Sample construction

- Selection date: 2026-09-11T18:40:44Z. Method: for each of 10 languages (Python, JavaScript, TypeScript, Go, Rust, Java, C++, C#, Ruby, PHP) and 4 star buckets (100-499, 500-1999, 2000-9999, 10000+), the top 50 repositories by stars from the GitHub search API with `pushed:>=2026-06-01 archived:false fork:false`. The exact query strings are in `dataset/sample.json`.
- Selected 1993 repositories; scanned 1993; 1699 have at least one workflow file, 294 have none.
- Window: the 90 days before each repository's scan. Per repository: all workflow files, the file tree, up to 200 runs (the API's `total_count` for the window is recorded separately), and the jobs of up to 8 runs (newest completed run of each workflow first, then newest overall).
- Public repositories only, REST API only, aggregate results only. Job-level minutes are therefore a **sample**, not the full window; percentages are of the sample, and 90-day totals are labelled estimates.

## 1. How prevalent is each optimisation, versus the 2024 academic baseline?

Baseline: Bouzenia & Pradel, ICSE 2024 (https://software-lab.org/publications/icse2024_workflows.pdf), paid-tier repositories, 30 months of history.

| measure | this corpus | ICSE 2024 |
|---|---:|---:|
| repositories using a dependency cache in any job | 71.8 % of 1699 | 32.9 % |
| … among repositories with an install step | 71.5 % of 1371 | — |
| repositories setting `timeout-minutes` on any job | 27.1 % | 14.0 % |
| repositories setting `timeout-minutes` on every job | 3.1 % | — |
| PR workflows with `concurrency` + `cancel-in-progress` (repos) | 34.2 % of 1549 | — |
| workflow files with a `paths`/`paths-ignore` filter | 20.9 % of 13215 | — |
| repositories with a scheduled workflow | 45.7 % | — |
| completed runs that failed | 12.8 % of 231608 | 17.4 % |
| sampled billed minutes in failed jobs | 5.3 % | 30.9 % (VM time) |
| scheduled runs, share of completed runs | 11.0 % | 29.8 % |
| scheduled jobs, share of sampled minutes | 19.8 % | 15.4 % |
| scheduled runs that failed | 12.8 % | 13.0 % |
| repositories with a dead cron (H001, ≥3 consecutive scheduled failures) | 132 of 776 with schedules | — |

The corpus is public repositories priced at private list rates; the baseline is paid-tier (private) repositories. Comparable in kind, not in population.

## 2. How much sampled CI time is attributable to each waste pattern?

Sampled: 56,682 jobs from 11,608 runs, 374,329 billed minutes (346,089 minutes of actual execution). Recoverable minutes are ciburn's per-finding estimates, summed; methods are in `dataset/findings.csv`; estimates for different rules can overlap.

| rule | repos flagged | measured findings | recoverable minutes (est.) | % of sampled billed minutes |
|---|---:|---:|---:|---:|
| H001 | 132 (7.8 %) | 156 | 4,525 | 1.2 % |
| H002 | 494 (29.1 %) | 785 | 52,574 | 14.0 % |
| H003 | 51 (3.0 %) | 65 | 3,869 | 1.0 % |
| H004 | 2 (0.1 %) | 2 | 327 | 0.1 % |
| H005 | 13 (0.8 %) | 15 | 1,966 | 0.5 % |
| H006 | 130 (7.7 %) | 156 | 20,644 | 5.5 % |
| H007 | 0 (0.0 %) | 0 | 0 | 0.0 % |
| H008 | 39 (2.3 %) | 60 | 0 | 0.0 % |
| H009 | 0 (0.0 %) | 0 | 0 | 0.0 % |
| W000 | 5 (0.3 %) | 0 | 0 | 0.0 % |
| W001 | 1352 (79.6 %) | 1635 | 3,266 | 0.9 % |
| W002 | 884 (52.0 %) | 1559 | 1,934 | 0.5 % |
| W003 | 1635 (96.2 %) | 7947 | 1,154 | 0.3 % |
| W004 | 1347 (79.3 %) | 0 | 0 | 0.0 % |
| W005 | 734 (43.2 %) | 847 | 10,439 | 2.8 % |
| W006 | 463 (27.3 %) | 490 | 470 | 0.1 % |
| W007 | 576 (33.9 %) | 611 | 0 | 0.0 % |
| W008 | 121 (7.1 %) | 24 | 8,090 | 2.2 % |
| W009 | 577 (34.0 %) | 794 | 2,972 | 0.8 % |
| W010 | 852 (50.1 %) | 951 | 0 | 0.0 % |
| W011 | 46 (2.7 %) | 0 | 0 | 0.0 % |
| W012 | 593 (34.9 %) | 658 | 10,718 | 2.9 % |

Other measured shares of sampled billed minutes: failed jobs 5.3 %, run attempts ≥ 2 (re-runs) 5.6 %, scheduled jobs 19.8 %. Queue time was 53.2 % of execution time (not billed).

## 3. How does per-job minute rounding change the picture?

GitHub rounds every job up to a whole minute. In the sample, 28,240 of 374,329 billed minutes (7.5 %) are rounding: time billed but not executed. Per repository the median share is 21.7 % and the 90th percentile 63.1 %. 47.6 % of sampled jobs ran under one minute and 63.3 % under two; every one of them bills a whole minute per leg.

## 4. What would the sample cost at private-repository rates?

At 2026 list prices the sampled jobs would cost $4,243.72; at the 2025 rates $5,673.09 (-25.2 %). Extrapolating each repository's sampled minutes-per-run to its 90-day run count gives a median of $13.27 per repository per 90 days, p90 $1,334.71, max $248,877.23, sum $2,377,980.33 (estimate: assumes the sampled runs are typical of the window).

Runner mix of sampled minutes: 87.5 % on GitHub-hosted runners with a known SKU, 0.6 % on GitHub-hosted runners with an org-defined label (unpriced), 11.8 % on self-hosted or third-party runners ($0 under the models in effect).

## 5. GitHub's "96 % of customers will see no change" statement

GitHub said (2025-12-16) that 96 % of customers would see no bill change from the 2026 pricing update. This corpus cannot test that claim: it sees no bills, no included-minute drawdown, and no private usage. What it can say, at list price and for the runners it can identify:

- 99.9 % of repositories with priced minutes have a lower 2026 list price than 2025 for the same sampled minutes; 0.1 % are unchanged (only arm64 2-core Linux kept its price).
- The postponed $0.002/minute self-hosted charge would have added $88.69 to the sampled minutes on self-hosted/third-party runners, 2.1 % of the sample's 2026 hosted list price (public repositories would have stayed free).

## Limitations and what this data cannot support

- Job-level data is a sample of at most 8 runs per repository; run listings are capped at 200 per repository (917 repositories exceeded it). Absolute minute totals are therefore not window totals.
- The sample is the most-starred repositories per language/bucket that were pushed recently, not a random sample of GitHub.
- Public repositories: their standard-runner minutes cost nothing on GitHub. Every dollar figure is a list-price equivalent.
- Larger Linux/Windows runners use org-defined labels and are unpriced here; macOS large/xlarge labels are priced.
- Recoverable-minute estimates are per-rule scenarios with stated methods; they overlap and are upper bounds where noted.
- Nothing here measures anyone's bill, included-minute consumption, or the effect of the postponed self-hosted charge on real accounts.

## Reproduce

```bash
GITHUB_TOKEN=... uv run python scripts/corpus_scan.py --workdir corpus --per-cell 50
uv run python scripts/build_report.py --workdir corpus --out dataset --report REPORT.md
```

Schema: `dataset/SCHEMA.md`. Headline numbers: `dataset/summary.json`.
